services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: offerflow
    restart: unless-stopped

    ports:
      - "127.0.0.1:3000:3000"

    env_file:
      - .env

    networks:
      - offerflow-network

networks:
  offerflow-network:
    external: true